profiles:
  - name: bad-profile
    media_type: video
    quality_profile: balanced
    postprocessings:
      - type: metadata_text
        parameters:
          filename: metadata.json
      - type: export_info_json
